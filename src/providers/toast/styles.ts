import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    left: 0,
    position: 'absolute',
    right: 0,
    top: 0,
    zIndex: 100,
  },
  content: {
    gap: 8,
    paddingHorizontal: 16,
    paddingTop: 16,
  },
});
